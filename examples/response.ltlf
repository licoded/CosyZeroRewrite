# Response Pattern
# When a request is made, eventually acknowledge
# Variables: quest (request), ack (acknowledgment)
# Note: using 'quest' instead of 'req' because parser has bug with 'r'

G(!quest | F(ack))
